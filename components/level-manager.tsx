"use client";
import { useState } from "react";
import { Toast } from "./ui";
export function LevelManager({levels}:{levels:{id:string;name:string;isActive:boolean}[]}){const [items,setItems]=useState(levels);const [note,setNote]=useState('');async function save(id:string,name:string){const r=await fetch('/api/levels',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'rename',id,name})});const d=await r.json();setNote(d.message||d.error);if(r.ok)setItems(xs=>xs.map(x=>x.id===id?{...x,name}:x));setTimeout(()=>setNote(''),1500)}return <section className="card setting-card"><h2>Level harga</h2><p className="muted">Nama level dapat Anda ubah kapan saja. Tambah level dari tombol + di topbar.</p>{items.map(l=><div className="inline-fields" style={{marginTop:12}} key={l.id}><label className="field" style={{margin:0}}><input defaultValue={l.name} onBlur={e=>e.target.value!==l.name&&save(l.id,e.target.value)}/></label>{l.isActive&&<span className="tag">Aktif</span>}</div>)}{note&&<Toast message={note}/>}</section>}
