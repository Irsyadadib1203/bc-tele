import { redirect } from "next/navigation";
import { currentUserId } from "@/lib/auth";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{error?:string}> }) { if(await currentUserId()) redirect("/dashboard"); const {error}=await searchParams; return <main className="auth-shell"><section className="login-card"><div className="logo"><span className="logo-mark">✦</span>Bcastly</div><div className="eyebrow" style={{marginTop:34}}>Telegram broadcaster</div><h1>Selamat datang kembali</h1><p className="muted">Masuk untuk mengelola katalog dan broadcast produk Anda.</p><form action="/api/auth/login" method="post"><label className="field">Username<input name="username" required placeholder="Masukkan username" /></label><label className="field">Password<input name="password" type="password" required placeholder="Masukkan password" /></label>{error&&<p style={{color:"#d7395c",fontSize:12}}>{error}</p>}<button className="btn btn-primary" type="submit">Masuk ke dashboard →</button></form><div className="hint">Akun awal: <b>admin</b> / <b>admin123</b>. Ubah password akun ini lewat database setelah login.</div></section></main>; }
