import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { currentUserId } from "@/lib/auth";
export async function POST(req:Request){if(!await currentUserId())return NextResponse.json({error:"Tidak diizinkan"},{status:401});const {action,id,name,apiKey}=await req.json();if(action==='create'){if(!name?.trim())return NextResponse.json({error:"Nama level wajib diisi"},{status:400});const level=await prisma.priceLevel.create({data:{name:name.trim()}});const settings=await prisma.settings.findUnique({where:{id:1}});if(!settings?.selectedLevelId)await prisma.settings.upsert({where:{id:1},update:{selectedLevelId:level.id},create:{id:1,selectedLevelId:level.id}});return NextResponse.json({message:"Level harga ditambahkan"})}if(action==='select'&&id){await prisma.settings.upsert({where:{id:1},update:{selectedLevelId:id},create:{id:1,selectedLevelId:id}});return NextResponse.json({message:"Level harga dipilih"})}if(action==='update'&&id){if(!name?.trim())return NextResponse.json({error:"Nama level wajib diisi"},{status:400});await prisma.priceLevel.update({where:{id},data:{name:name.trim(),apiKey:apiKey||null}});return NextResponse.json({message:"Level dan API key diperbarui"})}if(action==='delete'&&id){const count=await prisma.priceLevel.count();if(count<=1)return NextResponse.json({error:"Minimal harus ada satu level harga"},{status:400});await prisma.productCategory.deleteMany({where:{levelId:id}});await prisma.priceLevel.delete({where:{id}});const settings=await prisma.settings.findUnique({where:{id:1}});if(settings?.selectedLevelId===id){const fallback=await prisma.priceLevel.findFirst({orderBy:{createdAt:'asc'}});await prisma.settings.update({where:{id:1},data:{selectedLevelId:fallback?.id||null}})}return NextResponse.json({message:"Level harga dihapus"})}return NextResponse.json({error:"Permintaan tidak valid"},{status:400})}
